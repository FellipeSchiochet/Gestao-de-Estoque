@inherits LayoutComponentBase
@inject ITokenStorageService TokenStorageService
@inject NavigationManager NavigationManager

<MudThemeProvider Theme="_theme" />
<MudDialogProvider />
<MudPopoverProvider />
<MudSnackbarProvider />

<MudLayout Class="app-shell">
    <MudDrawer @bind-Open="_drawerOpen"
               Variant="DrawerVariant.Mini"
               Elevation="0"
               Class="app-drawer">
        <div class="app-drawer__inner">
            <div class="app-drawer__brand">
                <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" OnClick="ToggleDrawer" />
                @if (_drawerOpen)
                {
                    <div class="app-drawer__logo">EC</div>
                    <MudText Typo="Typo.subtitle1" Class="app-drawer__title">Gestão de Estoque</MudText>
                }
            </div>

            <MudNavMenu Class="app-nav">
                <MudNavLink Href="/main" Match="NavLinkMatch.All" Icon="@Icons.Material.Filled.Dashboard">
                    @if (_drawerOpen) { <span>Dashboard</span> }
                </MudNavLink>
                <MudNavLink Href="/produtos" Match="NavLinkMatch.Prefix" Icon="@Icons.Material.Filled.Inventory2">
                    @if (_drawerOpen) { <span>Produtos</span> }
                </MudNavLink>
                <MudNavLink Href="/produtos/novo" Match="NavLinkMatch.Prefix" Icon="@Icons.Material.Filled.AddBox">
                    @if (_drawerOpen) { <span>Novo produto</span> }
                </MudNavLink>
                <MudNavLink Href="/movimentacoes" Match="NavLinkMatch.Prefix" Icon="@Icons.Material.Filled.SwapHoriz">
                    @if (_drawerOpen) { <span>Movimentacoes</span> }
                </MudNavLink>
                <MudNavLink Href="/movimentacoes/historico" Match="NavLinkMatch.Prefix" Icon="@Icons.Material.Filled.Timeline">
                    @if (_drawerOpen) { <span>Historico</span> }
                </MudNavLink>
            </MudNavMenu>

            <div class="app-drawer__spacer"></div>

            <div class="app-drawer__footer">
                @if (_drawerOpen)
                {
                    <MudButton FullWidth="true" Variant="Variant.Filled" Color="Color.Primary" OnClick="SairAsync"
                               StartIcon="@Icons.Material.Filled.Logout">
                        Sair
                    </MudButton>
                }
                else
                {
                    <MudIconButton Icon="@Icons.Material.Filled.Logout" Color="Color.Primary" OnClick="SairAsync" />
                }
            </div>
        </div>
    </MudDrawer>

    <MudMainContent>
        <MudContainer MaxWidth="MaxWidth.False" Class="app-content">
            @Body
        </MudContainer>
    </MudMainContent>
</MudLayout>

@code {
    private bool _drawerOpen = true;

    private readonly MudTheme _theme = new()
    {
        PaletteLight = new PaletteLight
        {
            Primary = "#0F3D75",
            Secondary = "#2563EB",
            Background = "#F5F7FB",
            Surface = "#FFFFFF",
            AppbarBackground = "#FFFFFF",
            AppbarText = "#172033",
            DrawerBackground = "#FFFFFF",
            DrawerText = "#172033",
            DrawerIcon = "#66758F",
            Success = "#15803D",
            Warning = "#D97706",
            Error = "#C62828",
            Info = "#2563EB",
            TextPrimary = "#172033",
            TextSecondary = "#66758F",
            LinesDefault = "#E6EBF2",
            TableStriped = "#FAFBFD"
        },
        LayoutProperties = new LayoutProperties
        {
            DefaultBorderRadius = "14px"
        }
    };

    private void ToggleDrawer()
    {
        _drawerOpen = !_drawerOpen;
    }

    private async Task SairAsync()
    {
        await TokenStorageService.RemoverTokenAsync();
        NavigationManager.NavigateTo("/login?loggedout=true", forceLoad: true);
    }
}
